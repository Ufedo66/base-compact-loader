[project]
name = "base-compact-loader"
authors = ["Developer Team"]
description = "A secure, efficient system for compact data loading and verification"
telemetry = false
requirements = []
[contracts.compact-loader]
path = "contracts/compact-loader.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
